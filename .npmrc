# The Pear/Bare/QVAC dependency graph has intentionally loose (and sometimes
# conflicting) peer ranges — e.g. b4a's optional react-native peer pulls a newer
# React than the web app uses. legacy-peer-deps keeps installs deterministic and
# reproducible for judges without forcing unrelated upgrades.
legacy-peer-deps=true
